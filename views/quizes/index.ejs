<form class="search" action="/quizes" method="get">
  <input type="text" name="search" placeholder="Buscar pregunta">
  <input type="submit" value="Buscar">
</form>
<table>
  <% var i; for (i=0; i< quizes.length; i++) { %>
    <tr>
      <td>
        <a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a>
        <span>[categoría: <%= quizes[i].tema %>]</span>
      </td>
      <%if(session.user){%>
      <td>
        <a href="quizes/<%= quizes[i].id %>/edit">
          <button type="button">editar</button>
        </a>
      </td>
      <td>
        <form class="delete" action="quizes/<%= quizes[i].id %>?_method=delete" method="post">
          <button type="submit" onclick="return confirm('Borrar: <%= quizes[i].pregunta %>')">borrar</button>
        </form>
        </a>
      </td>
      <%}%>
    </tr>
  <% } %>
</table>
<%if(session.user){%>
  <a href="/quizes/new">
    <button type="button">Crear pregunta</button>
  </a>
<%}%>
